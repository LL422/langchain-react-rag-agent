# ==============================================
# DevBot — LLM Configuration
# ==============================================

# Chat model (DeepSeek, OpenAI, DashScope, LM Studio, etc.)
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-your-deepseek-api-key

# Embedding model (can be different from chat provider)
# Uses LLM_BASE_URL / LLM_API_KEY as fallback if not set
# EMBED_BASE_URL=http://localhost:1234/v1
# EMBED_API_KEY=not-needed
